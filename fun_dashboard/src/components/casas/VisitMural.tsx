import type { HouseView } from "@/lib/types";
export function VisitMural({ visits }: { visits: HouseView["mural"] }) { return <section className="rounded-2xl border border-zinc-200 bg-white p-4 dark:border-zinc-800 dark:bg-zinc-900"><h2 className="font-semibold">📌 Mural de visitas</h2>{visits.length ? <ul className="mt-3 space-y-2 text-sm text-zinc-600 dark:text-zinc-300">{visits.map((visit, index) => <li key={visit.id || index} className="rounded-lg bg-zinc-50 p-2 dark:bg-zinc-800"><b>{visit.nickname || "Visitante"}</b>{visit.note ? ": " + visit.note : " passou por aqui"}</li>)}</ul> : <p className="mt-2 text-sm text-zinc-500">Ainda sem visitas.</p>}</section>; }
