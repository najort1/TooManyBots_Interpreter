import type { HouseView } from "@/lib/types";
export function HouseCard({ house, host }: { house: HouseView["house"]; host?: string }) { return <section className="rounded-2xl border border-zinc-200 bg-white p-4 shadow-sm dark:border-zinc-800 dark:bg-zinc-900"><p className="text-xs font-semibold uppercase tracking-wider text-zinc-500">{host || "Sua casa"}</p><div className="mt-2 flex items-end justify-between"><span className="text-3xl">🏠</span><div className="text-right text-sm text-zinc-600 dark:text-zinc-300"><div>Limpeza: {house.cleanliness}%</div><div>Segurança: {house.securityLevel}/3</div></div></div></section>; }
