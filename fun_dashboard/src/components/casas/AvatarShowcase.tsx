import type { AvatarState } from "@/lib/types";
export function AvatarShowcase({ avatar, editable = false, onSelect }: { avatar: AvatarState | { slots: Record<string, string>; level: number }; editable?: boolean; onSelect?: (id: string) => void }) { const labels: Record<string, string> = { hair_face: "Rosto", outfit: "Roupa", optional_accessory: "Acessório" }; const catalog = "catalog" in avatar ? avatar.catalog : []; return <section className="rounded-2xl border border-zinc-200 bg-white p-4 shadow-sm dark:border-zinc-800 dark:bg-zinc-900"><div className="flex items-center gap-4"><div className="grid h-20 w-20 place-items-center rounded-full bg-violet-100 text-4xl dark:bg-violet-950">🧍</div><div className="text-sm text-zinc-600 dark:text-zinc-300">{Object.entries(labels).map(([slot, label]) => <p key={slot}>{label}: <b>{avatar.slots[slot] || "—"}</b></p>)}</div></div>{editable && <div className="mt-4 grid gap-2 sm:grid-cols-2">{catalog.map((item) => <button key={item.id} type="button" disabled={!item.owned} onClick={() => onSelect?.(item.id)} className="rounded-lg border border-zinc-200 p-2 text-left text-sm disabled:cursor-not-allowed disabled:opacity-45 dark:border-zinc-700"><span className="mr-2">{item.emoji}</span>{item.name}<span className="float-right text-xs">{item.owned ? "Equipar" : item.cost ? item.cost + "c" : "Nv. " + item.unlockLevel}</span></button>)}</div>}</section>; }
