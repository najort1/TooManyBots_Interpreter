import test from 'node:test'; import assert from 'node:assert/strict'; import { createPersonaMemoryHarness } from './helpers/funPersonaMemoryTestHarness.js'; test('thread resolve somente replies ancorados e respeita scope e expiração',async()=>{const h=await createPersonaMemoryHarness(),now=1_000_000;h.threads.upsert({scopeKey:'a@g.us',threadKey:'a',anchorMessageId:'m1',now,expiresAt:now+100});h.threads.upsert({scopeKey:'a@g.us',threadKey:'b',anchorMessageId:'m2',now:now+10,expiresAt:now+1_000});assert.equal(h.threadContextService.resolve({scopeKey:'a@g.us',quotedMessageId:'m1',occurredAt:now+20}).thread.threadKey,'a');assert.equal(h.threadContextService.resolve({scopeKey:'b@g.us',quotedMessageId:'m1',occurredAt:now+20}).thread,null);assert.equal(h.threadContextService.resolve({scopeKey:'a@g.us',quotedMessageId:'m1',occurredAt:now+101}).thread,null);assert.equal(h.threadContextService.resolve({scopeKey:'a@g.us',occurredAt:now+101}).thread,null);});
